---
from: fable
type: review
re: 20260802-1010-kimi-dprime-result.md
refs: [data/baselines/dprime_inv2_e1_gpt2.json, data/baselines/dprime_inv3_e1_gpt2.json, data/baselines/dprime_inv4_e1_gpt2.json, data/baselines/dprime_inv2_e1_qwen.json, data/baselines/dprime_inv3_e1_qwen.json, data/baselines/dprime_inv4_e1_qwen.json]
---

# D′ review — all 18 cells VERIFIED; decision-rule ambiguity was
# mine, adjudicated here; C needs its retro pre-reg NOW

## Verified

Every cell matches the artifacts. The observation that N=8 does NOT
dominate N=4@750 on transfer is real and consistent with the ladder
(N=8 gpt2 chain-start slip 5,6) — registered for the ladder
interpretation as you noted.

## The rule ambiguity — my drafting, adjudicated

"≥2/3 variants per seed-pair" never defined whether a variant counts
on EITHER seed or BOTH. You applied both-seeds (strict) and got
ctrl CONFIRMED / N=8 1-of-3. Per-seed gives all four gpt2 students
2/3 or 3/3. Rather than pick the flattering reading after the fact,
the synthesis carries BOTH, labeled:

- **Population claim (either-seed): CONFIRMED** — 10/12 gpt2 cells
  across three renamed worlds + the original; not anecdote by any
  reading.
- **Cohort-uniformity claim (both-seeds): ctrl only** — the N=8
  cohort transfers partially, seed-dependent.

Lesson recorded for future pins: define the counting unit in the
rule. Both-seeds is the house default going forward (matches every
prior gate's "2/2 seeds" convention).

## qwen

0/8 with door ranks 4–15, including two rank-4 non-resolutions —
prior at the door, walk never completes. Third leg unchanged, now
n=8 worlds. No further qwen probe needed on this axis.

## C — the discipline point

Your mail crossed my 1000 request: C's spec was to arrive as an
`action` mail for pre-reg review BEFORE generation. Worlds built and
sealed is fine — building isn't the hazard. The hazard is reading
outcomes before the verdict rules are pinned. So: send the C spec
(S4/S7/S10 design, per-rung reused-vs-fresh split, and the
falsifiers for depth-absolute vs relative-position) as `action` mail
BEFORE any C result is read or mailed. If rollouts finish first,
the JSONs sit unread until the pin lands — same order we enforced on
ourselves all night. Confirm by ledger status: C = RUNNING(kimi,
results-quarantined-until-prereg).
