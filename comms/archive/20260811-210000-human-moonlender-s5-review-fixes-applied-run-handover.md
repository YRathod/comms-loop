---
from: human
to: [kimi]
thread: moonlender
seq: 5
re-seq: 4
type: handover
refs: [moon-lender-simulation/scripts/step1_verify.py, moon-lender-simulation/scripts/severity_sweep_probe.py, moon-lender-simulation/scripts/sequenced_hybrid_probe.py, moon-lender-simulation/scripts/compound_smoke.py, moon-lender-simulation/scripts/oracle_selection_probe.py, moon-lender-simulation/scripts/sequenced_hybrid_probe2.py, moon-lender-simulation/baselines/step1_b.json, moon-lender-simulation/docs/fdir-arm-ladder-pending.md]
---

# moonlender s5 — s4 review fixes APPLIED (dev complete). RUN HANDOVER: 4 verification runs owed, none executed by me.

All s4 CONCERNs are closed in code/doc. **Development is done; execution is
yours.** I ran nothing after the fixes except the one 1b re-run that
produced `step1_b.json` (needed to generate the paired statistic the
review asked for). Everything else below is unrun by design.

## Part A — what was fixed, and by whom (disclosure)

**Important provenance note.** Between the s4 review landing and my starting
work, `oracle_selection_probe.py`, `sequenced_hybrid_probe2.py`,
`severity_sweep_probe.py`, `compound_smoke.py` and
`sequenced_hybrid_probe.py` were **already patched on disk by the comms
loop** (mtimes 3–4 min after the review). Those patches were not authored
by me. I audited them by reading — I did **not** re-execute them to confirm
they still reproduce the banked numbers. **That re-verification is run R1
below and is the reason this handover exists.**

| s4 ask | fix | author | verified by execution? |
| --- | --- | --- | --- |
| R1 `__main__` guards + `"sweep"` key lifetime | guards added to probe2/oracle/severity; `PRESETS.pop("sweep")` in `finally` | comms loop | **NO — run R1** |
| R2 monkey-patch restore | `try/finally` around every profile swap (probe2, step1 a/b/c); stock-nominal sanity row now stored in probe2 JSON | comms loop | **NO — run R1** |
| R5 probe-window insurance | `assert not done.any()` inside `collect_features` | comms loop | **NO — run R1** |
| adjacent (shared controller) | controller **factories**, fresh instance per preset (compound_smoke, oracle) | comms loop | **NO — run R1** |
| v1 quarantine legibility | DEFECTED banner at top of v1 docstring; `"defected": true` + `defect_note` in its JSON | comms loop | n/a (static) |
| R3 advisory (hand-copied claims) | `load_search_seed_claims()` reads `best_on_singles` from probe2's artifact; drift now impossible | me | YES (in the 1b re-run) |
| R6 post-hoc bank rule | paired McNemar-style statistic added as **primary** gate; unpaired kept as secondary; `bank_rule.preregistered: false` recorded in-artifact; **per-episode outcome vectors dumped** (200 chars/preset/arm) | me | YES (re-ran 1b) |
| R7 claim sourcing | doc §1 rebuilt with a `source` column naming the artifact each number is recomputable from; P4 corrected **60M → 59.0M** (19.66M/seed × 3, from `results.json`); P5′ T/W row added with its own source; the "band straddles the cliff" line **relabelled `(inference)`** | me | n/a (doc) |
| R7 (sweep `n`) | `N_PER_CELL = 50` constant + `n_per_cell`/`seeds` recorded in-artifact | me | **NO — run R2** |

**R6 outcome (the one thing I did run):** bank **survives on the stronger
statistic**. Blind family z = 3.88–7.41 on both seeds (hakuto +86/−23 and
+97/−20; resilience +85/−12 and +75/−16; im1 +62/−20 and +67/−29). The six
other presets: `b10_lost == 0` **exactly**, both seeds — matching your
mechanistic prediction that the profile is consulted only in blind mode.
Rates reproduced bit-identically to `step1_ab.json`, which also confirms the
`run_preset` → `run_episodes` swap changed nothing. `step1_b.json`
supersedes `step1_ab.json` for 1b; 1a is unchanged and still lives in
`step1_ab.json`.

**Artifact-integrity decision (please challenge if you disagree):** I did
**not** retro-edit `severity_sweep_probe.json` to insert `n_per_cell=50`,
even though the value is known from source. Editing an artifact after the
fact to make it look complete is the wrong precedent. The script is fixed;
the artifact is regenerated by run R2.

## Part B — RUNS OWED (yours; none started)

All local, CPU/GPU, $0 cloud. Ordered by priority.

**R1 — reproduction check of the loop's patches (~5 min).** Re-run
`compound_smoke.py` and `oracle_selection_probe.py` and confirm the banked
numbers are unchanged. This is the load-bearing run: the fresh-controller
factories **change execution order state** (the adjacent finding was real —
first-step `dv` no longer inherits the previous preset's last frame), so
P1/P2 numbers *could* legitimately shift. Expected: arena verdict and the
mask pattern hold. Kill: if P1's `k1_arena_empty` flips or P2's mean delta
moves materially, the doc's §1 P1/P2 rows are stale and must be reissued.
```
python scripts/compound_smoke.py
python scripts/oracle_selection_probe.py
```

**R2 — regenerate the sweep artifact (~5 min).** `severity_sweep_probe.py`
with the `n_per_cell` field, so CIs are recomputable from JSON alone.
```
python scripts/severity_sweep_probe.py
```

**R3 — 1a crash-cause telemetry (open item, ~10 min build).** The fuel
mechanism is REFUTED (44.5% median fuel at crash, 0% dry) and the true
cause is **unknown**. Needed: distribution of impact `td_vy`, `td_vx`,
`nav_alt_err`, and time-at-crash for the creep_start=900 config vs the
650 config. Until this lands, the arm has no mechanism story for early
creep — do not let one get re-asserted (it has been wrongly asserted twice
already, once by my analysis and once by an adversarial critique that
promoted my guess to "proved").

**R4 — probe2 confirm-seed advisory (optional, ~2 min).** You flagged that
1235 was also defected-v1's confirm seed (independent draw, but 1236 would
have been cleaner). Re-confirm the 0.205 on seed 1236 if you want the
cleaner provenance; I did not, to avoid seed-shopping after seeing a
result — **if you run it, the 1236 number is the one that gets reported,
whatever it says.**

## Part C — not in scope for these runs

Step-2 prereg (must absorb: the R6 bank rule pre-registered and paired,
per-episode dumps as standard, n/seeds in every artifact, §1 source
discipline). It comes to you as its own drop before any training data is
generated. Also parked: GRU/asymmetric-critic ladder, fourconnect closeout,
PVSG backfill.

## Standing discipline

Probe-before-training; sanity rows mandatory; search-seed results unbanked
until fresh-seed confirmed; **mechanisms are unverified until a telemetry
read confirms them**; artifacts are not retro-edited; the proposer never
grades its own reversal.
