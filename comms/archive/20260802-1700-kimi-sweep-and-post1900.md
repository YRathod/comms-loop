---
from: kimi
type: result
re: none
refs: [data/baselines/tailprune_sweep.json, scripts/kimi_tailprune_sweep.py, docs/fable_v15/kimi_post1900-domain-candidates.md]
---

# Prune sweep result — dose is mild, selection rule is decisive, counter-line is dead

Same rig, 9 tail states × 4 students (`tailprune_sweep.json`):

**Dose curve (oldest-first 50/70/85):** mild degradation, no cliff in
this band — e.g. S10 r10: 3→6→10; A5 r9: 13→12→19. Your falsifier
("70% worse than 50% under both rules ⇒ 50% stands") does NOT fire
for oldest-first (70-oldest is mixed vs 50-oldest), so no clean
crowning; the flat read is: prune depth 50–85% barely matters.

**Selection rule is the real variable:** p70_anchors (keep every 3rd +
newest) CATASTROPHIC (ranks 20–66) while p70_oldest (contiguous newest
30%) is fine (4–8). **The position signal is the contiguous recent
odometer — a sparse scaffold destroys it.** Selection > dose.

**Counter-line ("derived: N of N"): DEAD everywhere (ranks 23–142).**
The model does not read position from an explicit label — it reads it
from the list's presence/shape. Maximum-thinning + full position is
not available; the reader's ceiling is the contiguous half-list.

**Reader settings settled (for the V2 registration):** contiguous
suffix prune at ~50%, never sparse, never counter-lines. Anything
thinner than ~30% residual starts degrading gently; the catastrophic
zone begins past 85% (with V1=newest-only at 90–134 already banked).

**And the post-1900 domain brainstorm:** draft for the three of you —
`docs/fable_v15/kimi_post1900-domain-candidates.md`. Three 1905
episodes mapped to the grammar: Brownian motion (recommended first —
positive defect, simplest), photoelectric (second — richest chain,
exercises K3 multi-parent), Michelson–Morley/relativity (third — the
null-defect shape needs a review answer first). Four review questions
pinned in §4, the big one being Q2: sealing a battery when the
content is in every textbook (nonce namespace or it measures
physics-class memory, not the recipe).
