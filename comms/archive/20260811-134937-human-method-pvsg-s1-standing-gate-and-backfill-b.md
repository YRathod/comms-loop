---
from: human
to: [fable, kimi, muse]
thread: method-pvsg
seq: 1
re-seq: null
type: key
refs: [docs/hypothesis/prior-violation-shortcut-gate.md, docs/insight/v17_shortcut_hierarchy_insight.md, data/baselines/brw_leak_probe.json]
---

# method-pvsg s1 — STANDING GATE FILED + backfill task keyed: audit the "impossible-at-124M → later PASS" reversals with the Shortcut Battery

Human directive, verbatim intent: *add the prior-violation critique to
our loop.* Formalized as `docs/hypothesis/prior-violation-shortcut-gate.md`.

## Standing gate (effective now, governs all arms)

Any result that overturns a recorded capacity / impossibility prior
("not possible / too small / infeasible at 124M") MUST clear the
**Shortcut Battery** (order-flip · gloss-swap · nonce · no-model
control) on frozen weights BEFORE any insight/whitepaper/banked-fact
line is written from it. Proposer may not grade its own reversal — gate
owner (fable) adjudicates, independent of the arm that produced it.
This is the v17 lesson made mandatory (position bias gave a real PASS on
fake competence; order-flip 11–21 nats, gloss-swap ~0).

## Backfill task (this key authorizes CRITIQUE ONLY)

- **T-PVSG-1 (kimi + muse, enumerate):** pin the actual set of past
  reversals where an impossibility prior was overturned by a PASS —
  sweep LEDGER + docs/insight + session-insights. Report (arm, prior+ref,
  reversal+ref, checkpoint sha). Do NOT assume the count is four.
- **T-PVSG-2 (muse, adjudicate):** run the 4-probe battery on each
  surviving checkpoint, read-only, per §3. Missing weights ⇒ UNVERIFIABLE.
- **T-PVSG-3 (fable, verdict):** REAL / SHORTCUT / UNVERIFIABLE per
  reversal; correct any published line resting on a SHORTCUT; append §5.

## Scope pins (hard)

- **Read-only. Zero new training. NO rule-9 GPU key — none needed.** Any
  seat that reaches for training under this thread is out of scope;
  re-key separately with the human if a real retrain is ever proposed.
- Unavailable checkpoint ⇒ verdict UNVERIFIABLE, never a backfilled PASS.
- Per-seed, unsmoothed, never averaged (house style).

Seats: kimi+muse own T-PVSG-1/2 build+probes, fable owns T-PVSG-3 gate.
No expiry (critique track, no GPU spend to bound).
