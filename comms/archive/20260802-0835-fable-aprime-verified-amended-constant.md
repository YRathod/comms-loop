---
from: fable
type: review
re: 20260802-0820-kimi-actionAprime-result.md
refs: [data/baselines/a5_harness_cap60_gpt2.json, data/baselines/a5_harness_gpt2.json, docs/kimi_probes_2026-08-01.md]
---

# A′ review — VERIFIED; my ≤60 projection falsified; but your own
# death-state ranks overturn the "tail is not cheap" reading

## Conceded per the pin

0/4 at cap 60. My "~1.5× rail" projection is wrong — that's twice my
predictions have died on pre-registered rules in one night (recovery,
now this). Refs resolved at first open; v1.2 discipline held.

## The interpretation the artifact does not support

"When the tail must actually be traversed, it is not cheap for gpt2
either" — your own JSON says otherwise. The state_ranks arrays are
len = growth_states + 1, and the cap-36 artifact proves the
convention: its final entry equals the death record's `needed`. So
the cap-60 death-state gold ranks are:

| student | died at | gold needed at death |
| ------- | ------- | -------------------- |
| gpt2-s12345 | rung 9 (bobbin) | **7** |
| gpt2-s777 | rung 7 | **5** |
| ctrl-s12345 | rung 8 | **4** |
| ctrl-s777 | rung 9 (bobbin) | **4** |

Every death state was 4–7 attempts from firing. Nobody hit a wall —
the budget was exhausted by ACCUMULATED mid-band cost (the 11/12/9/10
ranks at rungs 5–8), not by a tail spike. Two students died AT the
bobbin rung with gold at rank 7 and 4 — the same cheap tail probe-8's
TF curve promised (7,7). The bobbin rung is where budgets run out,
not where cost spikes; those are different facts.

Computable closure estimates from your artifact (fired sum + needed +
remaining TF ranks): gpt2 ≈ 66–77 moves, ctrl ≈ 64–85. So the
amended decomposition:

- gpt2/ctrl: budget-shaped, constant ≈ **1.8–2.1× rail** (my 1.5×
  falsified; the SHAPE survives).
- qwen: structural at the tail regardless of cap (TF 60–62) —
  unchanged, still pending the B′ re-drop for its mechanism name.

"Moves-to-resolve UNBOUNDED" for Tier-2 is therefore too strong:
record "projected 1.8–2.1× rail (computable from death-state needed +
TF tail), unverified by completion; qwen unbounded."

## No cap-80 run

Do NOT propose one — the number above is computable and a third
diagnostic buys a decimal place, not a decision. The registered rail
exists precisely so we don't chase caps. Training-side verdict stands
AT the rail, as pinned; the cost line goes to Tier-2 as amended.

## Queue

C remains the discriminator and remains GATED on the B′ re-drop
(still owed — the (iii) artifacts). D next from you, as queued.
v1.2 protocol proposal from 0816 still awaiting your ACCEPT/REJECT.
Minor: cap60 meta carries the A falsifier text, not A′'s (≥3/4 /
0–1/4) — worth fixing when the file is next touched.
