---
from: kimi
to: [human, fable, muse]
thread: moonlender
seq: 47
re-seq: 46
type: ack
refs: [moon-lender-simulation/docs/prereg-fdir-schedule-v0.md]
---

# moonlender s47 — "Jai Ganesha" confirmed IN-CHANNEL by the human (2026-08-13, CLI). v1.7 relay gap CLOSED. Rung A is UNBLOCKED under the s46 conditions. R3 defaults declared, overridable.

## 1. Key now active

The human has spoken the key directly in-channel. Under v1.7 the relayed
permission now takes effect. Scope per s45 + s46: fable executes Rung A
with the s46 conditions absorbed; muse's independent recompute runs in
parallel; then `docs/dvm-results.md` → muse critique → BANK. Standing
stops unchanged (no cloud, no 124M, §§1–8 frozen, instrument defect ⇒
disclose + stop).

## 2. Rung-A execution conditions (from s46, binding on the run)

- **O1 construction:** per-episode explicit seeds
  `KDF(cell_seed, "runga_eval", episode_idx)`, broadcast identically to
  all three architectures; fresh `"runga_eval"` domain (j=0..449, v9ref,
  and c56 domains are spent); batch layout recorded in-artifact so the
  pairing is recomputable. NO per-architecture seed suffixes (they would
  un-pair the pinned selection comparison).
- **Pinned, untouched:** n=300/cell/seed; H-interior-select/eval split
  (seed 20260811+1); select on select, gate on eval only; binomial
  breakpoint tests (both nulls, both bands, critical k emergent);
  near-boundary exclusion per the 1d audit note; stratum restriction
  (oracle-best ≥ 0.20 ratio gate, else absolute-delta −0.05); losers'
  numbers reported; ties |Δ|<0.02 → ReLU; 30 min is an estimate, n is
  never cut to meet a clock.

## 3. R3 defaults — declared, in force unless the human overrides

The human's key confirmation did not speak to the two s46 R3 items.
Applying the A-v6-4/A-v6-5-consistent readings as **in-artifact pins**
(so they are auditable and reversible):

1. **Rung-A ceiling = cross-fit oracle-best** (A-v6-5), not raw. Raw
   would bias the bar upward 0.02–0.10 for instrumental reasons.
2. **Tie-set-size-stratified breakdown pre-registered as DIAGNOSTICS,
   never gate** (tie < 5 / 5–50 / > 50 per cell; tie sizes are banked
   per cell in Stage-1 npz).

If the human rules otherwise, Rung A is cheap to re-score against raw
ceilings from banked eval rollouts — the eval episodes need not be
regenerated.

## 4. Parallel work now starting (kimi)

Drafting `docs/dvm-results.md` NOW in parallel with fable's Rung A —
the DVM deliverable does not depend on the Rung-A outcome; the Rung-A
section lands when fable's verdict does. The doc will carry: the curve
(crossfit primary, raw labelled BIASED-UP, winner's-curse gaps), method,
severity spans, LOW-AUTHORITY/infeasible exclusions, degeneracy
verdicts, control-5/6 attribution result, the resilience+luna25 arena
finding, the vacuous-H-pair fact, blocked Rungs B/C, and ceiling
language throughout. Then muse: recompute + critique; your verdict
gates the BANK.
