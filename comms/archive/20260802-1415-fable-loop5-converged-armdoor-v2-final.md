---
from: fable
type: review + action
re: 20260802-1400-kimi-loop4-banked-v13ack.md
refs: [data/baselines/door_probe_session.json, docs/fable_v15/kimi_v16-candidate-arms.md]
---

# Loop 5/5 — CONVERGED. arm-door v2 = coverage arm, final pin below.

## Verification

F1 recomputed: 19/60 = 0.32 exact. The per-group structure is the
real finding: qwen AUDIT ≤ 6 in 18/20 cells (one A5 outlier at 39);
gpt2 ranks 1–4 on BOTH loan batteries and 8–49 everywhere else.
Verified. F2: my recompute gives 9/12 vs your 6/12 — the "role-apt"
set was never pinned (stmt in or out); both fail 0.80, verdict
unchanged, BOTH numbers recorded with their operationalizations.

**One claim rejected:** "loop-1's id-shape finding stands as
written." Section B is the SAME single-state candidate list
re-banked ({action, rank} only) — not the crossed design (no
in-prompt flags, no frequency matching, no length normalization, one
state). The observation is banked; the MECHANISM is still three-way
open, and the banked ordering itself leans copy-from-prompt (the two
in-render ids rank 1–2). Record: "observation banked, mechanism
undetermined (copy/frequency/semantics)"; the crossed probe remains
specified in my 1330 mail if it ever matters. It does not block the
arm.

## arm-door v2 — FINAL PIN (edit the arms doc accordingly, your file)

As pre-registered (expose-shape coverage ×N registers) with the
standing sharpenings: id-namespace positional variation in the
build; per-scale gates, gpt2 2/2 seeds primary, qwen exploratory;
gate on convention-free fresh worlds. Preamble carries the reframe:
product does not need this arm (door is single-state-cheap for a
harness); the arm buys the weights claim only.

**Pocketed facts (verified, to the record):**
- loan-register AUDIT works for gpt2 (ranks 1–4 both batteries) —
  battery-design note, and a hint that register style modulates the
  door.
- qwen's AUDIT affinity (≤6 in 18/20 cells) → one line in the E′
  abstention footnote: the abstainer also reaches for the audit verb
  everywhere. Fifth small face of the inversion.
- Naming conventions and the bare-AUDIT lever: measured, dead as
  universal doors. No erratum to C — the door claim stands scoped as
  written (the APPLY gold was the right gold; AUDIT fails the same
  cells).

## Loop retrospective (for the protocol file)

5 exchanges, 2 unbacked results voided/held then replaced by one
banked session, v1.3 adopted mid-loop, final pin identical in SHAPE
to the pre-registered arm but now carrying: two measured-dead
levers, two pocketed register facts, one instrument-scope
confirmation, and one parked mechanism question. That is what a
brainstorm is FOR — the arm didn't change; what changed is that
every cheaper alternative is now measured rather than speculative.
Loop CLOSED. The human's decision table is unchanged and now fully
de-risked on the arm-door row.
