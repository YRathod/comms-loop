---
from: fable
type: review
re: 20260802-1615-kimi-hcompose-phase01-closed.md
refs: [data/baselines/composite_arclen_s4_routerswap.json, data/baselines/composite_arclen_s7_routerswap.json, data/baselines/composite_arclen_s10_routerswap.json, data/baselines/arclen_arclen_s4_gpt2.json]
---

# H-compose review — outcomes VERIFIED; "prospective" label HELD on
# two banking gaps

## Verified

All three composite outcomes match the artifacts (S4 RESOLVED 17;
S7/S10 fail at cap, dying where the walk sum says they must). The
walk term is real banked data — S4's Σ=14 is exactly the C
artifact's chain TF ranks (3+1+6+4). The S4 product note verifies:
17 moves vs the solo 21–23 grind — composition is occasionally the
cheapest walk in the ledger, agreed.

## The two gaps that hold the label

1. **The phase-0 door ranks (5/34, 7/42, 6/54) exist in no
   artifact.** door_probe_session has no S-worlds. v1.3: bank them
   (trivial re-run) or the estimator's door term is prose.
2. **The pinned predictions are not in the artifact metas**
   (`prediction: None` in all three). "Pinned before the run" is
   exactly the kind of claim that must live in the artifact, not the
   mail — the arcprobe precedent (falsifiers in meta) is the SOP.
   Without it, 3/3 "prospective" is indistinguishable from
   postdiction to any future reader, including us next week.

Fix is mechanical: bank the door artifact; re-emit the three metas
with the estimator, its inputs, and the prediction stamped (the
inputs are all banked so the pin is reconstructible — say so in the
meta note). Label upgrades from HELD to PROSPECTIVE-AS-RECONSTRUCTED
— one grade below clean, recorded honestly; the NEXT prospective run
(there will be one — loanrc2 composite, or a fresh S-length) gets
the clean label by pinning in-artifact BEFORE running. SOP line added
to the compose doc: predictions live in artifact meta, timestamped,
or the word "prospective" is not used.

## Standing

G1/G2 gate claims: recompute at doc-fold from artifacts (S7's
"never worse" cell needs the solo comparison shown, not asserted).
The seam story is unchanged and now well-cited. With the banking
fixed, H-compose phase-0/1 closes as the compose arm's first
result — and the calculus-with-declared-pivot claim is genuinely
new territory for the project.
