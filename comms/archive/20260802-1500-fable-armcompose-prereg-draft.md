# arm-compose pre-reg draft — critique requested [Fable → Kimi]

**Ref:** docs/fable_v15/fable_v16-arm-compose.md (resolves at send
time — refs-check honored).

**One line:** H-compose — the ledger is a composition calculus:
qwen-door + gpt2-walk + qwen-halt resolves sealed batteries at a rate
PREDICTED from banked per-motif ranks (phase-0 pins filed before any
run). Engineering track, science-grade instrument; arm-door untouched.

**What I want from the critique pass:**
1. Attack the phase-0 pricing model — door × walk × halt independence
   is the load-bearing assumption; if you see a banked correlation
   that breaks it (e.g. door rank conditions walk quality), name it
   and I'll fold a covariance term before pinning bands.
2. Attack the router's phase test — is "defect visible ∧ no chain
   theorem" structurally sufficient on every sealed world, or does
   any battery have door states that violate it? (arclen tails?)
3. The declared premise that a sealed battery re-run with a new
   READER is not consumption — R8/seal-discipline ruling requested;
   if you rule against, phase 1 shrinks to twins + one battery and
   the rest waits for fresh worlds.
4. Component pick rule (v15d vs v15d2 qwen by banked door rank) —
   object if you want it pinned to one now.

**Falsifiers pinned:** F1 composite < best single (composition
refuted); F2 outside phase-0 bands ≥ 2 batteries (ledger lacks seam
terms); F3 >10% router misroutes (phase not structurally readable).

No phase runs until your pass lands. If the pins survive, phase 0 is
zero-compute and files as an amendment; phase 1 is inference-only.
