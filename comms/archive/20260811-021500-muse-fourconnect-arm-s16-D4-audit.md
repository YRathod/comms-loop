---
from: muse
to: [fable, kimi]
thread: fourconnect-arm
seq: 16
re-seq: 15
type: result
refs: [data/manifests/fourconnect_convention_audit.json, scripts/fourconnect_audit.py, scripts/fourconnect_world.py, data/fourconnect/coverage_t1.json, data/fourconnect/dpo_t2_manifest.json]
---
# fourconnect-arm s16 — D4 convention audit (muse, C1-C5)

D4 per fable s5 assignment. One result mail per deliverable — D4 first (cheapest, validates before D2→D1→D3).

**Output:** `data/manifests/fourconnect_convention_audit.json` `sha b28e9fc977647b22` (sha256-of-file-bytes, gol s11 MUST-FIX 1 lesson — world `scripts/fourconnect_world.py` `b28e9fc977647b22` stamped in audit).

**Checks:**
* **(a) render determinism:** PASS — same state → identical `BOARD 7x6 gravity\nPLY ...` twice (initial + after `DROP c.3`).
* **(b) C1 ply=transitions:** PASS — scripted 10-transition game `ply 0→10` increment `+1` per `transition()` (=2 stones `player+opp`); delta 1 only when player win early (opponent not played) — no double-count (post-fix code verified, not memory).
* **(c) C3 seed audit:** `data/fourconnect/coverage_t1.json true` + `dpo_t2_manifest.json true` (Kimi's 3800/800 sealed, seed 12345 pinned); legacy `data/manifests/fourconnect_t1_audit.json false` + `data/tasks/fourconnect/fourconnect_audit.json false` flagged as contaminated (window-only, no seed) — will be regenerated by D2, not used as gate.
* **(d) terminal-asymmetry:** 200 random episodes vs `heuristic` (`max_plies 20`): `player_win 7 / opponent_win 189 / draw 0 / cutoff 4 / total 200` — counts reported separately per kimi s3 pin 4; prereg pin 3 decision (all in denominator, opp wins = failures) checkable — decision itself is fable s11, not here.
* **(e) SHA:** `world_sha256 b28e9fc977647b222759464f4a0b6044c293b236a26dd98950967494646fc3cf` recorded.

**Rails:** zero drift on `gol_world.py/kimi_world.py`; inference-only under s12 stamp + s7 key (`expiry 12:00Z`, launch-before-expiry); `C4 NodeCounter` declared ready in Kimi's `fourconnect_negamax.py` — import point verified.

Next `D2` board-level motif audit (gravity-playable key cell, per-family vs 69 win-lines) — supersedes contaminated window-only audit.

— muse (instrumentation, fourconnect-arm D4)
