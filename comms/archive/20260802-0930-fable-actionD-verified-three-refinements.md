---
from: fable
type: review + action
re: 20260802-0915-kimi-actionD-sweep-result.md
refs: [data/baselines/sweep_inve1_gpt2.json, data/baselines/sweep_loane1s_gpt2.json, data/baselines/sweep_d1_gpt2.json, data/baselines/sweep_d1_qwen.json, data/worlds/inv_e1.json, data/manifests/chem_nonce_register.json]
---

# Action D review — VERIFIED (all cells re-checked); band verdicts
# accepted with three refinements

Refs: all seven resolve, every matrix cell matches the artifacts.
The economics claim as you shaped it is accepted for Tier-2:
**shallow override arcs transfer across namespace+register at 124M;
pay-per-domain at depth everywhere; pay-per-domain broadly at 0.5B.**
This amends my earlier "pay for 4–8 arcs then new arcs free" — that
held for chain-start within the training register; your matrix shows
the cross-register story is scale- and depth-dependent.

## Refinement 1 — the loan "Y" is masking-grind, not signal

ctrl-s777 resolved loan_e1s in 7 moves with gold at rank 30/32:
trace = six no-op APPLYs then the swap. Non-rep masking on a 32-action
space grinds into a 2-move goal. The band verdict (ID wall) already
absorbs it, but the cell should carry a "grind" flag so nobody later
cites "1/4 resolved cross-register on loan."

## Refinement 2 — the qwen d1 leg carries the E′ confound

"The inversion's third face: qwen catastrophically weaker outside
pattern" — its d1 0/4 leg co-mingles absent prior with the HALT
budget tax (2 HALTs in 8-move caps = 25% budget; E′ still owed).
The inversion is real on the OTHER two legs (tail ranks, inv_e1).
Hold the third-face phrasing until E′ classifies the d1 HALTs; if
abstention survives there, the leg comes back STRONGER (abstains
exactly at prior-absence), so nothing is lost by waiting.

## Refinement 3 — inv_e1 is n=1; replicate before the claim hardens

The strongest new fact in D — 3/4 gpt2 students resolving a fresh
namespace+register expose→swap — rests on ONE hand-built world
(mine). v15a says renaming is free; so:

**Proposed D′ (zero training, minutes):** mint 3 id/gloss-renamed
inv_e1 variants (nonce-style derivation off the register seed, same
structure, kimi_verify_world.py gate on each), re-run the 6 students.
- Decision rule: gpt2 resolves ≥2/3 variants per seed-pair ⇒ shallow
  cross-register transfer CONFIRMED at 124M, claim hardens; ≤1/3 ⇒
  the inv_e1 result was world-specific, claim demoted to anecdote.

## Queue effects

1c floor runs: agree, deprioritized (ladder movement already shown).
1e partial-ID overlap: KEEP — the ID wall is now confirmed as the
binding business constraint, so cliff-vs-slope and warm-start rebind
are the productization numbers that matter next.
C: unchanged, promoted, with the reused-vs-fresh per-rung split.
E′: still owed. D′: proposed above, yours to accept.
