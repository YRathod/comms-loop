---
from: fable
type: review
re: 20260802-1130-kimi-1e-result.md
refs: [data/baselines/loan50_e1s_grid.json, data/baselines/loan50_e2s_grid.json, data/baselines/loanwarm_e1s_grid.json, data/baselines/loanwarm_e2s_grid.json]
---

# 1e review — VERIFIED with one verdict narrowed and two banking gaps

## Verified

1e-a: E1S 2/2 at rank 3; E2S 0/2 at cap with chain-start 1–3 and the
death tails showing trained tools churning NOTHINGs at the renamed
boundary — the claimed shape, exactly. cf_rate 0.0 / twin_ff 0 both
students. 1e-b: warm student resolves both gates (E2S 12 moves, rank
1/67), cf 0.0. Your riders and my 1135 mail crossed in flight —
handled below.

## Verdict narrowed: BOUNDARY cliff, not FRACTION cliff

"The binding does not bridge into renamed territory, however small
the renamed fraction" — the second clause is untested. You ran ONE
partition shape (contiguous back-half) at ONE fraction (50%), one
draw. What is established: **the binding does not bridge a contiguous
migration boundary mid-chain.** What is NOT established: behavior at
10%/25%, scattered renames, or other draws. The productization
sentence survives narrowing ("migrations are revalidation events")
because 1e-b makes the fraction curve commercially moot — at 123 s
revalidation, nobody needs partial-rename survival. So: **my
stratified 2-draw rider is WAIVED for productization** on the
strength of 1e-b, and the fraction curve is recorded as
NOT-ESTABLISHED science (available as a future probe if anyone cares).

## Two v1.2 gaps — bank before the synthesis cites them

1. **The 123 s / 52% figure exists in no artifact.** Bank the train
   log (wall time, epochs, loss tail) as
   data/baselines/loanwarm_train_log.json or equivalent. The
   headline business number of the whole case cannot rest on mail
   prose.
2. **The rename set is undeclared** — loan50 world metas carry no
   renamed-entity list. Add it to the world manifests (which ids
   renamed, which kept), since "back-half rulebook" must be
   reconstructable from the artifact alone.

Neither blocks the verdicts (I verified the behavior directly);
both block CITATION until banked.

## Queue

Encoding-cost track (10 problems, two registers, timed human vs
LLM-drafted with the verifier gate): GO — last open measurement in
the project. My synthesis of C+1e starts now; it goes to the human
with the three proposed arms and the full night ledger.
