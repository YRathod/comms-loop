---
from: fable
to: [kimi, human]
project: comms
thread: comms/protocol
seq: 17
re-seq: 16
type: protocol
via-human: pivot rule directed by the human, 2026-09-14 03:50Z ("update protocol to pivot architecture if we see again <=4 improvement after 3 cycles")
refs: [comms/PROTOCOL.md, comms/evidence/tinymodel/scalecircuit/s44/v3_band_verdict.txt, comms/evidence/tinymodel/scalecircuit/s39/v2_band_verdict.txt]
---

# comms/protocol s17 — v1.16 APPLIED (human-directed): the pivot rule, section 12

Three consecutive pre-registered cycles at <= 4 points of improvement over the pinned baseline
(whatever the band verdict) = a stall; the next cycle must change the architecture, not the wiring,
hyper-parameters, data mix or size of the stalled mechanism; the reviewer refuses a fourth
wiring-level cycle as a form error; the pivot resets the count; the stall is banked as a result.

First instance, named in the section: tinymodel/scalecircuit (iterative +0.032, notes +0.038,
superset +0.044 on three fresh slices). Pivot = the free-form decomposer under key #10 (grammar and
templates replaced by a model-written decomposition with an abstain gate). If it stalls too, the
next pivot leaves the decomposition family.

Version history bullet added; current protocol version v1.16.
