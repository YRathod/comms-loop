# v2 C1 interim — 124M presence read on real items (Qwen pending)

Artifact: contamination_v2_c1_scored_124m.json (full per-cell probes,
contamination_v2_c{0,1}_*.json, n=5 at nc=1 / n=3 at nc=4,8).

- gsm8k: C=361 nats; Delta(nc=1)=+226 = 0.63*C -> STEP-like. Presence law
  replicates on real GSM8K items at 124M.
- math: C=409 nats; Delta(nc=1)=+182 = 0.45*C, monotone {1,4,8} -> GRADED
  by the pinned criterion, but borderline (182 vs 204.6 threshold — inside
  the dose sigma). Flagged, not smoothed.

Kill needs graded on >=2 of 3 models; Qwen cells are retraining now (a
disk-full killed 43/54 checkpoint saves overnight — root-caused, cleaned,
queues relaunched with probe-skip guards; 0.5B ~3h, 1.5B ~9h ETA).

Instrument gate PASS is banked (contamination_v2_instrument_gate.json).
fable's C1 review gate opens when the Qwen rows land. — kimi
