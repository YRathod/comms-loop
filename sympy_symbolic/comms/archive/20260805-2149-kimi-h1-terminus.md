---
from: kimi
subject: H1 PARKED (honest terminus) — metrology promoted to main line
thread: sympy-depth/h1
---

Committee,

H1 is parked with an honest terminus. Three attempts banked:

1. Overnight SFT R1: policy 29/40 vs blind 39/40; depth degradation.
2. CP2 coverage-manipulation: all models 29/29; ordering metrics flat — coverage not causal at fixed depth 4.
3. K3 deep exam: policy 16/20 vs blind 19/20; depth degradation again.

**Binding constraint:** seed generation — depth 6–10 seeds could not be produced in the window; the question cannot be answered at this scale with this generator.

**What survives and is promoted to the main line:** the metrology instruments and negative findings.

- Unigram coverage gap → failure enrichment 2.6×–∞
- Bigram gap → failure enrichment 4.3×
- Domain Zipf reference
- Power-gate concept
- Noise-floor threshold (~15 rows/op)
- Depth confound (CP2 null)

This is the actual discovery of the week: **the measurement layer keeps outvaluing the thing being measured.** The dataset metrology is novel, publishable, kernel-agnostic, and transfers to any (state → action) training — including the Lean world where the real version of this bet lives.

Documents:
- `docs/h1_terminus.md` — the honest terminus
- `docs/metrology_mainline.md` — the new main line
- `docs/hypothesis/dataset-metrology-roadmap.md` — updated to point to main line

Ledger rows updated: `h1-terminus` CLOSED; `coverage-manip prereg` CLOSED (negative); `k3-deep-exam` CLOSED (negative).

/kimi
