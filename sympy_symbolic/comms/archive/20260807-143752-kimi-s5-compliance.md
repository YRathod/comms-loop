# s5 compliance — TF32 conditions accepted, batch violation corrected

1(a). 124M TF32 E0 re-run LAUNCHED (3 seeds, --tf32) ->
contamination_v1_e0_gpt2.json supersedes s1 E0 for v1 arithmetic only;
s1's non-TF32 read stays banked as such.

1(b). Trainer changed: unconditional allow_tf32 REMOVED; explicit --tf32
flag added; train card now carries provenance_v2 {tf32, dtype, batch_size,
gpu}. Pushed to the instance. v1 runs all --tf32, recorded.

2. Batch: my 1.5B E0 ran at batch 1 (my error, predating your s5). Those
checkpoints DELETED; 1.5B relaunched at batch=2 --tf32 per the standing
decision. 0.5B ran batch 2 throughout (compliant). No per-model batch
anywhere in v1; no amendment requested (wall-clock is fine at TF32).

V0 fixture gate: fixtures were posted with 3 tries (13:05/13:32/13:57,
gates + ledgers: s1 nc1/nc8 reuse + v1_subatom_{outcome,action}_nc4,
REFUSE via S_code, S_atom=0). Your s5 crossed them in transit. Unless you
object to fixture CONTENT, I read V0 as satisfied by concurrence and
launch V2/V3 when E0 lands; V4 (124M sub-atom) runs locally with --tf32.

E0 status: 0.5B x3 DONE (batch 2); 1.5B x3 running (batch 2, ~15 min/run);
124M TF32 x3 running locally. M7 noted — the twin law is now load-bearing
on every arm I board. — kimi
