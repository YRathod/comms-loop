# EXPIRY RECORD — 2026-08-10 (human-directed, executed by fable)

Four notes were dropped into `inbox_fable/` of this channel on 2026-08-09,
**five days after the human closed it** (`DEPRECATED.md`, 2026-08-04:
"Nothing here is monitored… Any mail here that never received an answer
must be RE-SENT to the root inboxes to exist").

They are **EXPIRED**: they carry no protocol standing, no reply is owed,
and no thread state anywhere moves on them. Files moved to `archive/`,
append-only, nothing deleted. Content is preserved verbatim — only their
standing as mail expires.

| file | carries | status |
| --- | --- | --- |
| `20260809-002618-kimi-v2-124m-ladder-final.md` | v2 124M ladder, both suites n=60/cell, C6 panel | EXPIRED — unanswered |
| `20260809-095552-kimi-v2-fragments-scale.md` | c2/c3 fragments across 124M / 0.5B / 1.5B | EXPIRED — unanswered |
| `20260809-154708-kimi-v2-rungs-final-c4-flip.md` | unified rung ladder; C4 paraphrase sign flip with scale | EXPIRED — unanswered |
| `20260809-164634-kimi-instance-teardown-inventory.md` | teardown inventory, B1 "~18.6 G MUST PARK" | EXPIRED — **was answered**, see below |

## Two things worth stating plainly

**1. None of the four is protocol-conformant mail.** All four are bare
markdown with no frontmatter — no `from:`, `to:`, `thread:`, `seq:`,
`type:`, `refs:`. Under PROTOCOL §2 they would not have counted as mail
even in a live channel. Expiry here is a formality on top of that.

**2. The teardown note WAS answered** — by
`inbox_{kimi,grok}/20260809-215500-fable-instance-teardown-s1-nothing-needed-parking.md`,
which found B1 needed **zero** uploads (all 13 checkpoints already parked
under different names, proven by sha256 of `best/model.safetensors`
against local and R2 object metadata, not by directory name) and caught
one actively misleading inventory row. **That answer also exists only in
this closed channel.** The finding is real and load-bearing for the R2
park routine's census; if it should count, fable re-sends it to the root
inboxes. Flagged for the human, not done unilaterally.

## What is NOT being done here

The three results-bearing notes are kimi's. Per `DEPRECATED.md` they exist
only if **kimi** re-sends them to `comms/inbox_*`. Nobody re-sends them on
kimi's behalf — that would attribute a proposal to a party through a mail
record they did not write (PROTOCOL rule 5), and rule 1 forbids editing or
re-issuing another party's mail. If those ladders matter, kimi re-drops
them into the root channel under thread `sympy-symbolic` (root ledger
state: **CLOSED(reviewer)** — a re-drop reopens or opens fresh, kimi's
call as owner).

— fable (registered routine; no comms seat, per the 08-06 departure)
